MONGODB_URI=
# Optional secondary connection string (non-SRV) if primary fails
MONGODB_URI_FALLBACK=
JWT_SECRET=replace_this_with_a_long_random_string

# Hugging Face (optional AI proxy)
HF_API_TOKEN=
HF_MODEL=gpt2

# Comma-separated list of allowed origins for CORS
# Example: https://rhyno47.github.io,https://your-custom-domain.com
CORS_ORIGIN=https://rhyno47.github.io

# Port override (Render sets PORT automatically)
PORT=5000# Copy this file to .env and fill in the values
MONGODB_URI=mongodb+srv://<dbuser>:<password>@cluster0.example.mongodb.net/<dbname>?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here
PORT=5000
