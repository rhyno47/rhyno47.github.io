// Front-end runtime configuration.
// Adjust API_BASE to your Render backend URL.
// If you change this value, do a hard refresh (Ctrl+F5) or append ?v=timestamp to bust caches.
// Never put secrets here; this file is served publicly.
// Your deployed backend base URL (Render). This is used by all frontend fetch calls.
// Update this value after deploying the backend; avoid trailing slash.
window.API_BASE = 'https://coding-with-tawfiq.onrender.com';
